import { Origami } from "../types/global";
/**
 * Get's a list of origami modules with a specific type
 * @param type Type of module to find
 */
export declare const list: (type: Origami.ModuleType) => string[] | undefined;
